name: CI

on:
  push:
    branches: [main, master, "feature/**"]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Offline unit tests
        # Ниже мы безопасно передаем секреты из настроек GitHub в ваши тесты
        env:
          PROXY_API_KEY: ${{ secrets.PROXY_API_KEY }}
          # Если вашему проекту нужны другие ключи, раскомментируйте и добавьте их ниже:
          # ANOTHER_SECRET_KEY: ${{ secrets.ANOTHER_SECRET_KEY }}
        run: python -m unittest discover -s tests -v

      - name: Check git secrets history
        run: python scripts/check_git_secrets_history.py
        continue-on-error: true

  secrets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install pre-commit tools
        run: pip install pre-commit detect-secrets

      - name: detect-secrets (baseline)
        run: detect-secrets scan --baseline .secrets.baseline

      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # continue-on-error не даст сборке упасть, если сканер найдет ложные секреты в истории
        continue-on-error: true

